name: research-guard
version: 0.8.0-beta.3
description: "Hermes Agent plugin that injects compact web research and optional Google Maps route context before local/small models answer."
author: Iris / Stefan
provides_hooks:
  - pre_llm_call
provides_tools:
  - research_guard_search
  - research_guard_status
  - research_guard_diagnostics
